# سجل التغييرات — CHANGELOG

## 2026-09-03 — v4.0.0 (الإصدار النهائي)

### الترخيص الرقمي
- **ترخيص مربوط بالجهاز عبر الخادم**: جدول `license_state` في قاعدة البيانات — المعرف ثابت عبر كل المتصفحات والنوافذ وإعادة التشغيل (بدلاً من التخزين في المتصفح المتغير)
- راوتر عام `/api/license/status` + `/api/license/activate` (تحقق Ed25519 في الخادم عبر node:crypto)
- البوابة بمسارين: مسار الخادم (ويندوز) + مسار محلي (أندرويد) مع ترميم ذاتي لجدول الترخيص عند الإقلاع
- إصلاح «لا يدعم هذا الجهاز التحقق الآمن»: مسار noble الاحتياطي لعدم توفر WebCrypto Ed25519 + فحص الإصدار يرجع `version-mismatch` بدل `unsupported` المضلل
- إصلاح المسار المزدوج `/license/license/*` (404 دائم) وإدراج مفتاح التحقق العام داخل الحزمة

### المنافذ والشبكة المحلية
- منفذان ثابتان: الواجهة **41790** (0.0.0.0) وAPI **41789** — معرف الجهاز لم يعد يتغير بين التشغيلات
- CORS/CSRF لنطاقات LAN الخاصة + قواعد جدار حماية جاهزة

### الاستعادة والنسخ الاحتياطي
- **إصلاح جذري لفشل الاستعادة**: عبارات `ALTER TABLE transactions` كانت قبل إنشاء الجدول في `desktop-schema.sql` → أعمدة المزامنة مفقودة في القواعد الجديدة. نُقلت بعد الإنشاء + إزالة 8 backticks فاسدة من كتلة license_state
- ترميم ذاتي عند الإقلاع يضيف الأعمدة والفهرس وقيود FK إن غابت (يشفي القواعد القديمة)

### الحزم والإصدار
- حزمتا ويندوز (Offline + Protected) بإلكترون مطابق لـ v3.0.6 + حزمتا أندرويد موقعتان v1+v2
- مولّدات تفعيل (`KeyGenerator-v4.0.0-P-{Windows,Android}`) بتراخيص `appVersion="*"` تحفظ الترخيص في ملف نصي
- إصدار **Packages v4.0.0** منشور على GitHub Releases

### التنظيف
- حذف ~5GB ملفات مؤقتة وبيانات تطوير ومخلفات Replit وسكربتات اختبار مؤقتة (phase1-14)

---

## 2026-08-23

### إصدار Windows Portable `3.0.5`

- بناء نسخة Windows Portable من آخر commit على الفرع `main`.
- تبسيط إعدادات التشغيل المحلي (API مدمج مع قاعدة PGlite بدل PostgreSQL خارجي).
